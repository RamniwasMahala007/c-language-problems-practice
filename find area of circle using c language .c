#include<stdio.h>
#include<conio.h>
int main()
{
    int r;
    float a=3.14,area;
    printf("enter the radius of circle :");
    scanf("%d",&r);
    area=a*(r*r);
    printf("Area of circle is:%f",area);
    getch();

}
